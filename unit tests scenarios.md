## Unit tests scenarios
**Ready-to-go prompts**

- (case: **compare what have changed -> test exactly that**)

  Please add more test case (or test cases, as you reason) to this class to cover only those changes that happened to `SomeCustomBuilderClass` file, as compared to its previous version (the vctmp file attached to prompt context).
- (case: **see what the class do -> test it**)

  Please add some tests to cover main use cases from this class. Let the tests be stored in a new file. If you wonder about the format of tests we use, have a look at `ExemplaryTestClass.cs`.
- (case: **test is not working after my changes**)

  Please compare the changes I made to `MyServiceClass.cs`, as compared to its previous version, the vctmp file. Why is GivenWhenThenCustomTest failing? The production code changes were purposeful, so that it is advisable to adjust the test, rather than production code. The message is as follows: ...
